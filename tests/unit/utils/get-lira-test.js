import getLira from 'euprostir/utils/get-lira';
import { module, test } from 'qunit';

module('Unit | Utility | get lira');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getLira();
  assert.ok(result);
});
