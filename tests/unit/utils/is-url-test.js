import isUrl from 'euprostir/utils/is-url';
import { module, test } from 'qunit';

module('Unit | Utility | is url');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isUrl();
  assert.ok(result);
});
